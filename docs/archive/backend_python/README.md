# backend_python (legacy/archivo)

**Legacy/experimento;** no integrado con la aplicación actual.

El backend en producción es Node (Express, `server.js`). El Python activo del proyecto está en **`scripts/python/`** (utilidades: download_sensors, geocode_missing_nodes, harvest_dim_studies, etc.). Este directorio se archivó aquí para no perder el código pero queda fuera del árbol principal.
